{
    "name": "detective-client",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "predev": "node ../scripts/generate-manifest.js"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    },
    "dependencies": {
        "phaser": "^3.90.0"
    }
}
